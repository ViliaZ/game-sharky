class Keyboard {
    // arrow keys are only triggered by ONKEYDOWN and NOT by ONKEYPRESSED! --> use this in the function in game.js
    
    UP = false;
    DOWN = false;
    LEFT = false;
    RIGHT = false;
    KEYD = false;  // finslap
    SPACE = false; // bubble making (only possible after min. 1 coin is collected)
}